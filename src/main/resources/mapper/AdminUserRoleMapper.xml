<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.wj.dao.AdminUserRoleDao">
    <select id="listAllByUid" parameterType="Integer" resultType="Integer" >
        select rid from admin_user_role
        where uid=#{uid}
    </select>



</mapper>

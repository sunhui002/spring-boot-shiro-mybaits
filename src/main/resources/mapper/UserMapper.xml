<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.wj.dao.UserDao">
    <resultMap type="com.example.wj.pojo.User" id="usermap">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
       </resultMap>
    <resultMap type="com.example.wj.pojo.User" id="usermap2">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="enabled" property="enabled"/>
    </resultMap>
    <select id="findByUsername" parameterType="String" resultMap="usermap2">
        select id, username, password, salt,name, phone,email, enabled from user
         where username = #{username}
    </select>

    <select id="getByUsernameAndPassword" parameterType="String" resultMap="usermap2">
        select id, username, password, salt,name, phone,email, enabled from user
        where username = #{username} and password=#{password}
    </select>

    <insert id="save" parameterType="User">
        insert into user(username,password,salt)
        values(#{username},#{password},#{salt})
    </insert>

    <select id="findAll"  resultMap="usermap2">
        select id, username,name, phone,email, enabled from user
    </select>
</mapper>
